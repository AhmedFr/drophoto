export type DotLoaderProps = {
  /** Text shown next to the loader, in a small monospace/dim style. */
  label?: string;
  /** Pixel size of the (square) dot grid. Defaults to 18. */
  size?: number;
};
