/** Dark OpenFreeMap style — see the CSP's `connect-src`/`img-src` allowances in `src-tauri/tauri.conf.json`, which this URL's host must match. */
export const PLACES_MAP_STYLE_URL = "https://tiles.openfreemap.org/styles/dark";
